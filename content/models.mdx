---
title: 模型
description: OpenApi 可用模型,按协议与分组浏览、选择
---

import { ModelList } from '../components/ModelList'

# 模型

下面展示当前可调用的模型、各自支持的协议(OpenAI / Anthropic / Gemini)与所属分组。点击模型名可复制,填入请求的 `model` 字段即可。

<ModelList />

> 模型清单实时来自站点接口;具体价格与最新上架情况,请以站点的 [价格页](https://api.openrealm.cn/pricing) 为准。
>
> 想查看**某把 Key 实际可用**的模型?到 [在线调试](/playground) 填入 Key,点「获取我的可用模型」即可。
