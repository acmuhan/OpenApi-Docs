{
  "dependencies": {
    "next": "^16.2.7",
    "nextra": "^4.6.1",
    "nextra-theme-docs": "^4.6.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "scripts": {
    "dev": "next dev --webpack",
    "prebuild": "node scripts/fetch-pricing.mjs",
    "build": "next build",
    "postbuild": "pagefind --site out --output-subdir _pagefind",
    "preview": "npx --yes serve out"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  },
  "overrides": {
    "zod": "4.1.12"
  }
}
